var images = new Array();
$(
    function(){
        /*try{
            images = $("#var_images").attr("data-url").split(",");
        }catch(ex){}
        for(var i in images){
            var name = images[i];
            $('#'+name).fileupload(
                {
                    dataType: 'json',
                    done: function (e, data) {
                        console.log(data.result.files);
                        
                    }
                }
            );
        }*/
        
    }
);